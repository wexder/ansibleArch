---
- name: Install pipewire
  become_user: wexder
  become: true
  kewlfft.aur.aur:
    name:
      - pipewire

- name: Install pipewire dependencies
  become_user: wexder
  become: true
  kewlfft.aur.aur:
    name:
      - pipewire-audio
      - pipewire-alsa
